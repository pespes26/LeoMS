# Copy to .env and keep it private. Values here are not credentials.
TAILSCALE_IP=100.64.0.10
# Use 127.0.0.1 with Tailscale Serve TCP on macOS; use TAILSCALE_IP on Linux.
LEOMS_BIND_IP=127.0.0.1
LEOMS_ADMIN_USERNAME=owner
LEOMS_ADMIN_LOCAL_PORT=8080
RESTIC_REPOSITORY=s3:https://s3.example.invalid/leoms-backups
AWS_DEFAULT_REGION=us-east-1
